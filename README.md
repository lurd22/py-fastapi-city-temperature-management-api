# City Temperature API

## 📌 Overview

FastAPI приложение для:
- управления городами (CRUD)
- получения и хранения температуры для каждого города

---

## ⚙️ Setup

```bash
python -m venv venv
source venv/bin/activate  # Linux / Mac
venv\\Scripts\\activate   # Windows

pip install fastapi uvicorn sqlalchemy httpx